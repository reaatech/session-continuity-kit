# Redis Example

Demonstrates using the Redis storage adapter.

## Prerequisites

- Redis server running locally or accessible via REDIS_URL

## Running

```bash
export REDIS_URL=redis://localhost:6379
pnpm install
pnpm start
```
