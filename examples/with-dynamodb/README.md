# DynamoDB Example

Demonstrates using the DynamoDB storage adapter.

## Prerequisites

- AWS credentials configured
- DynamoDB table created

## Running

```bash
export AWS_REGION=us-east-1
export DYNAMODB_TABLE=sessions
pnpm install
pnpm start
```
