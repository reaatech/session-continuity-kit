# Firestore Example

Demonstrates using the Firestore storage adapter.

## Prerequisites

- GCP project with Firestore enabled
- Application Default Credentials configured

## Running

```bash
export GCP_PROJECT_ID=my-project
pnpm install
pnpm start
```
